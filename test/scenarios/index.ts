// Scenario engine
import { executeStory } from "./utils/scenario-engine";

export async function ScenarioTests() {
  const scenarioTests: any = [];

  for (const scenario of scenarioTests) {
    // await executeStory(
    //   scenario,
    //   // this
    // );
  }
}
